export interface RecommendExerciseProps {
  title: string;
  thumbnailUrl: string;
  videoUrl: string;
}

const RecommendExerciseContainer = ( {

}: RecommendExerciseProps) => {

  return (
    <div className="">

    </div>
  );
};

export default RecommendExerciseContainer;